providers:
  ollama:
    chat_model:
      physical_model: "${CHAT_MODEL}"
      temperature: 0.3
      max_tokens: 2048
    embedding_model:
      physical_model: "${EMBEDDING_MODEL}"
      temperature: 0.0
      max_tokens: 512

  gemini:
    chat_model:
      physical_model: "${GEMINI_CHAT_MODEL}"
      temperature: 0.3
      max_tokens: 2048
    embedding_model:
      physical_model: "${GEMINI_EMBEDDING_MODEL}"
      temperature: 0.0
      max_tokens: 512

  lmstudio:
    chat_model:
      physical_model: "${LMSTUDIO_CHAT_MODEL}"
      temperature: 0.3
      max_tokens: 2048
    embedding_model:
      physical_model: "${LMSTUDIO_EMBEDDING_MODEL}"
      temperature: 0.0
      max_tokens: 512
